含 IO流
含 算法

冒泡排序 向量。整数组：
    向量。
        逆历 0··-1；1》》i：
            志：假
            历至 i-1》》j：
                 若 向量·j 》 向量·j+1：
                      交换 向量·j，向量·j+1
                      志。为 真
       若 志 为 假：
            跳出
启。整数：
  n。整数
  q。整数组
  打印 n
  n。遍历》》 t：
     t。取输入
     q。 尾存 t
  冒泡排序 q
  q。
  遍历》》x：
     打印 x
  换行
  0
  
  
  # 名域的概念
  对象。类型
    函数
    控制
    属性 为 值
  另一对象。控制 》》寄存值
    动作 参数》》另一寄存值
    动作 寄存值，另一寄存值
        
